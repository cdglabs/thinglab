'Allow to shift-click a file menu entry to enter that directory'UserView understands: 'fileMenu: label directory: directory matching: match | fileNames index selected	[[self shiftKey  [ "if shift pressed, use server/local instead of default"		directory = ''''  [directory _ updateURL]		directory _ '''']].	while true do [		fileNames _ user fileStrings: directory matching: match.		index _ (Menu new stringFromVector: fileNames , ('' cancel '' + label + '' '')) fbug.		index < 1 or index > fileNames length  [false]		selected _ directory + (fileNames  index).		self shiftKey  [directory _ selected + ''/'']		selected]]' classified: 'file stuff'